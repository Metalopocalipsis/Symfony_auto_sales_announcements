{% extends 'base.html.twig' %}

{% block title %}Ad{% endblock %}

{% block body %}
    <h1>Ad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ad.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ ad.title }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ ad.date }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ ad.country }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ad_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('ad_edit', {'id': ad.id}) }}" class="btn btn-primary">edit</a>

    {{ include('ad/_delete_form.html.twig') }}
{% endblock %}
